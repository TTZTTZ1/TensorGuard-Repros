
input_tensor = torch.randn(1, 1, 4, 4, 4)
indices = torch.argmax(input_tensor, dim=1, keepdim=True)
max_unpool = torch.nn.MaxUnpool3d(2, 2, 2)(input_tensor, indices)
output = max_unpool.transpose(1, 2)
output_unpool = torch.nn.ReplicationPad3d(padding=0)(output)
max_unpool = torch.squeeze(max_unpool)
output_unpool = torch.nn.Upsample(scale_factor=8, mode='nearest')(max_unpool)
output_unpool = torch.squeeze(output_unpool)
output_unpool = torch.nn.AdaptiveAvgPool3d(1)(output_unpool)
output_unpool = torch.squeeze(output_unpool)
output = max_unpool.transpose(1, 2)
